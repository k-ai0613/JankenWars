# セキュリティ改善レポート

## 実施した修正

### 1. CORS設定の強化
- **変更前**: すべてのオリジン (`*`) を許可
- **変更後**: HTTP・Socket.IO の両方で許可オリジンを限定
  - 本番: `https://jankenwars.onrender.com`
  - 開発: `http://localhost:5173`, `http://localhost:5001`, `http://localhost:5000`
  - `ALLOWED_ORIGINS`（カンマ区切り）で上書き可能
- Socket.IO 側は長らく `origin: "*"` のままで、ゲーム機能はすべて Socket.IO 経由のため
  Express 側の制限が実質無効化されていた。`server/security.ts` の `getAllowedOrigins()` に統一済み。

### 2. セキュリティヘッダーの追加
以下のヘッダーを追加しました:
- `X-Content-Type-Options: nosniff` - MIMEタイプスニッフィング防止
- `X-Frame-Options: DENY` - クリックジャッキング攻撃防止
- `X-XSS-Protection: 1; mode=block` - XSS攻撃防止
- `Strict-Transport-Security` - HTTPS強制
- `Referrer-Policy` - リファラー情報の制御
- `Content-Security-Policy` - コンテンツセキュリティポリシー

### 3. 入力検証とサニタイゼーション
- **全 Socket.IO ハンドラのペイロード型検証** — 検証前の分割代入で `TypeError` が発生し、
  `uncaughtException` 経由で `process.exit(1)` に到達してサーバ全体が停止していた
- ユーザー名の検証 (1-20文字、英数字・アンダースコア・日本語)
- ルームIDの検証 (UUID形式)
- ゲーム移動データの検証
- HTMLエンティティのエスケープ
- ペイロードサイズ制限 (100KB)

### 4. レート制限
- API エンドポイント: 1分間に100リクエスト
- **Socket.IO イベント: ソケット単位・イベント単位の上限**（`room:create` は 1分10回など）
- 1ソケットあたりの同時ルーム数と、サーバ全体のルーム数に上限を設定
  - 以前は Socket.IO が完全に無制限で、1ソケットから300ルームを作成できた

### 5. サーバの権威性
- じゃんけんの勝敗判定を `shared/gameRules.ts` に集約し、サーバ・クライアントで同一コードを使用
  - 以前はサーバ側に勝敗判定がなく、負ける駒でも相手の駒を奪えた
- 終局後の着手、対局中の再戦要求、マッチメイキングの重複登録を拒否
- 切断時もルーム状態を正しく戻し、`game:force:end` を通知

### 6. 環境変数管理
- `.env.example`ファイルを作成
- 機密情報の外部化を推奨

## 残存する課題と推奨事項

### 高優先度
1. **認証・認可システムの実装**
   - ユーザー認証機能が未実装。ユーザー名は自己申告で、なりすましが可能
   - JWTまたはセッションベースの認証を推奨

2. **依存関係の更新（部分対応済み）**
   - `npm audit`: 48件 → 7件（critical 2→0 / high 25→1）
   - `engine.io` と `socket.io-parser` の本番直撃分は解消済み
   - 残り7件はメジャーバージョンアップが必要（`vite` 5→8、`react-router-dom` 6→7 ほか）

3. **HTTPS強制**
   - Renderでの本番環境でHTTPS強制を確認

### 中優先度
1. **データベースセキュリティ**
   - SQLインジェクション対策の確認
   - 接続の暗号化

2. **ロギングとモニタリング**
   - セキュリティイベントのログ記録
   - 異常検知システムの導入

3. **WebSocket通信の保護**
   - Socket.IOの認証機能実装
   - WSS（WebSocket Secure）の使用確認

### 低優先度
1. **CSRFトークン**
   - 未使用のまま残っていた CSRF ヘルパーは削除済み（セッションミドルウェアも未導入のため常に 403 を返す状態だった）
   - 現在 REST API は GET のみで状態を変更しないため保護対象がない。
     状態変更エンドポイントを追加する際に併せて実装すること

2. **セキュリティテスト**
   - ペネトレーションテストの実施
   - 自動セキュリティスキャンの導入

## 即座に実行すべきコマンド

```bash
# 実施済み: npm audit fix（48件 -> 7件）
# 残りはメジャーバージョンアップが必要なため未実施
npm audit

# 回帰テスト（セキュリティ修正の検証を含む）
npm test
```

## セキュリティベストプラクティス
1. 定期的な依存関係の更新
2. セキュリティパッチの迅速な適用
3. 最小権限の原則の適用
4. 定期的なセキュリティ監査
5. インシデント対応計画の策定